import { Fragment } from 'react';

const Spinner = () => {
    return (
        <Fragment>
            <i className="fa fa-spinner fa-spin fa-4x" style={{ width: '200px', margin: 'auto', display: 'block' }}></i>
        </Fragment>
    );
};

export default Spinner;
